import path from "path";

interface SequelizePathsConfig {
  config: string;
  migrationsPath: string;
  modelsPath: string;
  seedersPath: string;
}

const sequelizePathsConfig: SequelizePathsConfig = {
  config: path.resolve("src", "db", "config"),
  migrationsPath: path.resolve("src", "db", "migrations"),
  modelsPath: path.resolve("src", "db", "models"),
  seedersPath: path.resolve("src", "db", "seeders"),
};

export default sequelizePathsConfig;
